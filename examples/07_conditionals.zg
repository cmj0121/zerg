# 07 — if / else if / else.
fn sign(n: int) -> int {
    if n < 0 {
        return -1
    } else if n == 0 {
        return 0
    } else {
        return 1
    }
}

fn main() {
    print sign(-8)           # -1
    print sign(0)            # 0
    print sign(42)           # 1
}
