# requires: v0.7

fn risky() -> Result[int, str] {
    return Result.Err("nope")
}

fn worker() -> Result[int, str] {
    defer print "cleanup"
    let v := risky()?
    return Result.Ok(v + 1)
}

fn main() {
    let r := worker()
    print r
}

main()
