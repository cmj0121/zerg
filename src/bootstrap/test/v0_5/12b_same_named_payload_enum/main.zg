import "util"

enum Token { Eof, Ident(str) }

impl Token {
    pub fn label() -> str {
        return "main"
    }
}

mt := Token.Ident("x")
print mt.label()
ut := util.Token.Ident("y")
print ut.label()
