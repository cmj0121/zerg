import "util"

r := util.parse("good")
match r {
    Outcome.Ok(v) => print v
    Outcome.Err(e) => print e
}
s := util.parse("nope")
match s {
    Outcome.Ok(v) => print v
    Outcome.Err(e) => print e
}
