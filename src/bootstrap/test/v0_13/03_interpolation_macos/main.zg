# requires: v0.13
greet: list[byte] = ['h', 'i', '\n']
quiet: list[byte] = ['o', 'k', '\n']
asm {
	mov x0, #1
	mov x1, ${greet}
	mov x2, #3
	mov x16, #4
	svc #0x80
	mov x0, #1
	mov x1, ${quiet}
	mov x2, #3
	mov x16, #4
	svc #0x80
}
