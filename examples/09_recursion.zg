# 09 — Recursion: a function may call itself.
fn fib(n: int) -> int {
	return n if n < 2
	return fib(n - 1) + fib(n - 2)
}

fn factorial(n: int) -> int {
	return 1 if n <= 1
	return n * factorial(n - 1)
}

fn main() {
	print fib(10)      # 55
	print factorial(5) # 120
}
