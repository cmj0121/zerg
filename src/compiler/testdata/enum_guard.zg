enum Opt {
	None
	Some(int)
}
fn describe(o: Opt) -> int {
	return match o {
		Some(v) if v > 10 => 100
		Some(v) => v
		_ => -1
	}
}
fn main() {
	print describe(Some(5))
	print describe(Some(50))
	print describe(None)
}
