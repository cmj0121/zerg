import "util"

enum Status { Active, Done }

impl Status {
    pub fn label() -> str {
        return "main"
    }
}

let ms := Status.Active
print ms.label()
let us := util.Status.Done
print us.label()
