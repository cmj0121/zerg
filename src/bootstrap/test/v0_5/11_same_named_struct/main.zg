import "util"

struct Counter { tag: int }

impl Counter {
    pub fn show() -> str {
        return "main"
    }
}

mc := Counter { tag: 1 }
print mc.show()
uc := util.Counter { tag: 2 }
print uc.show()
