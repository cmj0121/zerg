# requires: v0.7

fn main() {
    wg := wait_group()
    ch := chan[int](5)
    wg.add(5)
    for i in 0..5 {
        spawn fn() {
            ch <- i
            wg.done()
        }()
    }
    wg.wait()
    close(ch)
    for v in ch {
        print v
    }
}

main()
