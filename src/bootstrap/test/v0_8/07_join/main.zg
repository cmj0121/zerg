# requires: v0.8
import "strings"

print strings.join(["a", "b", "c"], "-")
print strings.join(["only"], "-")
print strings.join(["a", "b"], "")
parts := strings.split("hello,world", ",")
print strings.join(parts, " ")
