# requires: v0.9
import "std/os"

fn maybe_int(b: bool) -> int {
    if b {
        return 42
    }
    x: int = os.exit(7)
    return x
}

fn main() -> int {
    y := maybe_int(false)
    print y
    return 0
}

main()
