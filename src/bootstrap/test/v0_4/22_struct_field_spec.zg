spec Printable {
    fn to_string() -> str
}

struct Counter { count: int }
impl Counter for Printable {
    fn to_string() -> str {
        return "counter"
    }
}

struct Holder { g: Printable }

c := Counter { count: 7 }
h := Holder { g: c }
print h.g.to_string()
