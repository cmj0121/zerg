# requires: v0.7

fn main() {
    out := chan[int](2)
    in_ch := chan[int]()
    select {
        out <- 42 -> { print "sent" }
        v := <- in_ch -> { print v }
    }
    r := <- out
    match r {
        v => print v
        nil => print -1
    }
}

main()
