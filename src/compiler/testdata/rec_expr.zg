enum Expr {
	Num(int)
	Add(Expr, Expr)
	Mul(Expr, Expr)
}
fn eval(e: Expr) -> int {
	return match e {
		Num(n) => n
		Add(a, b) => eval(a) + eval(b)
		Mul(a, b) => eval(a) * eval(b)
	}
}
fn main() {
	# (2 + 3) * 4 = 20
	print eval(Mul(Add(Num(2), Num(3)), Num(4)))
}
