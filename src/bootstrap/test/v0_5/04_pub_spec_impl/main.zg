import "util"

struct Greeting { word: str }

impl Greeting for util.Printable {
    pub fn to_string() -> str {
        return this.word
    }
}

g := Greeting { word: "hello" }
p: util.Printable = g
print p.to_string()
