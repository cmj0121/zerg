# requires: v0.6

spec Tagged {
    fn tag() -> str
}

struct Box[T] {
    value: T
}

impl[T] Box[T] for Tagged {
    pub fn tag() -> str {
        return "Box"
    }
}

bi: Box[int] = Box { value: 7 }
bs: Box[str] = Box { value: "hi" }
print bi.tag()
print bs.tag()
