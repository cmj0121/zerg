# requires: v0.7

fn main() {
    let ch := chan[int](5)
    spawn fn() {
        ch <- 1
        ch <- 2
        ch <- 3
        ch <- 4
        ch <- 5
        close(ch)
    }()
    for v in ch {
        print v
    }
}

main()
