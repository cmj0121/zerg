spec Hashable {
    fn hash() -> int {
        return 99
    }
}

struct A { x: int }
impl A for Hashable {}

struct B { x: int }
impl B for Hashable {
    fn hash() -> int {
        return this.x
    }
}

a := A { x: 1 }
b := B { x: 5 }
things: list[Hashable] = [a, b]
for t in things {
    print t.hash()
}
