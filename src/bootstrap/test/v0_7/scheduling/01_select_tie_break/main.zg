# requires: v0.7

fn main() {
    a := chan[int](1)
    b := chan[int](1)
    a <- 100
    b <- 200
    select {
        v := <- a -> { print v }
        v := <- b -> { print v }
    }
    select {
        v := <- a -> { print v }
        v := <- b -> { print v }
    }
}

main()
